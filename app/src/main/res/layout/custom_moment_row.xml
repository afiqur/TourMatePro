<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:id="@+id/moment_photo_row"
    android:layout_height="match_parent"
    android:orientation="horizontal">


    <TextView
        android:id="@+id/tvMomentDetails"
        android:layout_width="50dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_toStartOf="@+id/ivMomentImage"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:minHeight="100dp"
        android:text="ABCD"
        android:textColor="#000000"
        android:textSize="25sp" />

    <ProgressBar
        android:id="@+id/progressBar2"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/tvMomentDetails"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/tvMomentDetails"
        android:layout_weight="1" />

    <ImageView
        android:id="@+id/ivMomentImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:padding="2dp"
        android:scaleType="fitXY"
        android:src="@drawable/camera" />


</RelativeLayout>